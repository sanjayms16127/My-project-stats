<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Cricket Player Stats</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f2f2f2;
            padding: 20px;
        }

        h1 {
            text-align: center;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            background-color: white;
            margin-top: 20px;
        }

        th, td {
            border: 1px solid #333;
            padding: 10px;
            text-align: center;
        }

        th {
            background-color: #4CAF50;
            color: white;
        }

        .form-box {
            background-color: white;
            padding: 15px;
            margin-top: 20px;
        }

        input {
            padding: 5px;
            margin: 5px;
        }

        button {
            padding: 6px 12px;
            cursor: pointer;
        }
    </style>
</head>

<body>

<h1>🏏 Cricket Player Stats</h1>

<div class="form-box">
    <h3>Add Player</h3>
    <input type="text" id="name" placeholder="Player Name">
    <input type="number" id="matches" placeholder="Matches">
    <input type="number" id="runs" placeholder="Runs">
    <input type="number" id="wickets" placeholder="Wickets">
    <button onclick="addPlayer()">Add</button>
</div>

<table>
    <thead>
        <tr>
            <th>Name</th>
            <th>Matches</th>
            <th>Runs</th>
            <th>Wickets</th>
        </tr>
    </thead>
    <tbody id="playerTable">
        <tr>
            <td>Virat Kohli</td>
            <td>500</td>
            <td>26000</td>
            <td>4</td>
        </tr>
    </tbody>
</table>

<script>
    function addPlayer() {
        let name = document.getElementById("name").value;
        let matches = document.getElementById("matches").value;
        let runs = document.getElementById("runs").value;
        let wickets = document.getElementById("wickets").value;

        if (name === "" || matches === "" || runs === "" || wickets === "") {
            alert("Please fill all fields");
            return;
        }

        let table = document.getElementById("playerTable");
        let row = table.insertRow();

        row.insertCell(0).innerHTML = name;
        row.insertCell(1).innerHTML = matches;
        row.insertCell(2).innerHTML = runs;
        row.insertCell(3).innerHTML = wickets;

        // Clear inputs
        document.getElementById("name").value = "";
        document.getElementById("matches").value = "";
        document.getElementById("runs").value = "";
        document.getElementById("wickets").value = "";
    }
</script>

</body>
</html>
